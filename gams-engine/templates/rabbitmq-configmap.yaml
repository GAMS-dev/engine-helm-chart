apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "common.names.fullname" . }}-queue-definition
  namespace: {{ .Release.Namespace }}
data:
  definitions.json: |-
{{ (tpl (.Files.Get "definitions.json") .) | indent 4 }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "common.names.fullname" . }}-queue-advanced-config
  namespace: {{ .Release.Namespace }}
data:
  advanced.config: |-
{{ (tpl (.Files.Get "advanced.config") .) | indent 4 }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "common.names.fullname" . }}-queue-config
  namespace: {{ .Release.Namespace }}
data:
  rabbitmq.conf: |-
{{ (tpl (.Files.Get "rabbitmq.conf") .) | indent 4 }}

