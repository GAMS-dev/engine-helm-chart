{{- if .Values.broker.apiVersion2.enabled }}
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "gams-engine.prefixname" . }}-brokerv2-scaler
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gams-engine.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "gams-engine.prefixname" . }}-brokerv2
  minReplicas: 1
  maxReplicas: {{ .Values.broker.apiVersion2.maxReplicas }}
  targetCPUUtilizationPercentage: 90
{{- end }}
