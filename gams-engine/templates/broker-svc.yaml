{{- $annotations := dict }}

{{- if .Values.service.annotations }}
{{- $svc := include "common.tplvalues.render" (dict "value" .Values.service.annotations "context" $) | fromYaml }}
{{- $annotations = merge $annotations $svc }}
{{- end }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "common.names.fullname" . }}-broker
  namespace: {{ .Release.Namespace }}
  {{- if $annotations }}
  annotations:
    {{- toYaml $annotations | nindent 4 }}
  {{- end }}
spec:
  selector:
    app: {{ include "common.names.fullname" . }}-broker
  ports:
    - port: 80
      targetPort: 80
      nodePort: {{ .Values.service.nodePort }}
  type: {{ required "value 'service.type' is required" .Values.service.type }}
