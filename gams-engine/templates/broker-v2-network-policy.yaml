{{- if .Values.global.useNetworkPolicies }}
{{- if .Values.broker.apiVersion2.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "gams-engine.prefixname" . }}-brokerv2-np
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gams-engine.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      app: {{ include "gams-engine.prefixname" . }}-brokerv2
  policyTypes:
    - Egress
    - Ingress
  ingress:
    {{- if .Values.broker.apiVersion2.networkPolicy.ingress }}
    # Ingress rules
    {{- include "common.tplvalues.render" (dict "value" .Values.broker.apiVersion2.networkPolicy.ingress "context" $) | nindent 4 }}
    {{- end }}
    {{- if .Values.broker.apiVersion2.networkPolicy.extraIngress }}
    # Optional extra ingress rules
    {{- include "common.tplvalues.render" (dict "value" .Values.broker.apiVersion2.networkPolicy.extraIngress "context" $) | nindent 4 }}
    {{- end }}
  egress:
    - to:
      - podSelector:
          matchLabels:
            app: {{ include "gams-engine.prefixname" . }}-fwd-prxy
      ports:
        - protocol: TCP
          port: 8000
    - to:
      - podSelector:
          matchLabels:
            app: {{ include "gams-engine.prefixname" . }}-queue
      ports:
        - protocol: TCP
          port: 5672
    - to:
      - podSelector:
          matchLabels:
            app: {{ include "gams-engine.prefixname" . }}-pgbouncer
      ports:
        - protocol: TCP
          port: 6432
    - to:
      - podSelector:
          matchLabels:
            app: {{ include "gams-engine.prefixname" . }}-mongo
      ports:
        - protocol: TCP
          port: 27017
    - to:
      - namespaceSelector: {}
        podSelector:
          matchLabels:
            k8s-app: kube-dns
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    {{- if .Values.broker.apiVersion2.networkPolicy.extraEgress }}
    # Optional extra egress rules
    {{- include "common.tplvalues.render" (dict "value" .Values.broker.apiVersion2.networkPolicy.extraEgress "context" $) | nindent 4 }}
    {{- end }}
{{- end }}
{{- end }}
