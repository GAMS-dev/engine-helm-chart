apiVersion: v1
kind: Service
metadata:
  name: {{ include "gams-engine.prefixname" . }}-database-bouncer
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gams-engine.labels" . | nindent 4 }}
spec:
  selector:
    app: {{ include "gams-engine.prefixname" . }}-pgbouncer
  ports:
    - protocol: TCP
      port: 6432
      targetPort: 6432
